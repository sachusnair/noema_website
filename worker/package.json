{
  "name": "noema-chat-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker behind the site's chat widget. Holds the Anthropic key.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
